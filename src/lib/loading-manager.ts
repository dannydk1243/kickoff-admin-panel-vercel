// lib/loader-manager.ts

// This is a plain object that doesn't rely on React
export const loaderManager = {
  // We will "attach" the React setLoading function to this object later
  setLoading: (state: boolean) => {}, 
};